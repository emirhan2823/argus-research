import SwiftUI

// Bu dosya artık kullanılmamaktadır. Referans hatasını önlemek için tutulmaktadır.
// İçerik SettingsView.swift içindeki SettingsCommsView'a taşınmıştır.
struct WidgetListSettingsView: View {
    var body: some View {
        Text("Bu özellik Ayarlar > Comms altına taşındı.")
    }
}

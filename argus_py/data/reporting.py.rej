diff a/argus_py/data/reporting.py b/argus_py/data/reporting.py	(rejected hunks)
@@ -15,11 +15,13 @@ class Reporter:
         if not os.path.exists(self.run_dir):
             os.makedirs(self.run_dir)
         print(f"Logging run to: {self.run_dir}")
+        self.config = {}
             
     def save_config(self, args: Any):
         path = os.path.join(self.run_dir, "config.json")
+        self.config = vars(args) # Store for later use
         with open(path, 'w') as f:
-            json.dump(vars(args), f, indent=4)
+            json.dump(self.config, f, indent=4)
             
     def log_decision(self, verdict: ConsensusVerdict, mode_process: Any = None, capital_profile: str = "N/A"):
         path = os.path.join(self.run_dir, "decision_log.csv")
@@ -33,7 +35,11 @@ class Reporter:
                     "Mode", "CapitalProfile", "RiskStyle", "Lev", "Mult", 
                     "Votes", "Rationale",
                     "BlockReason", "AegeanSlope", "AegeanZone", "OrionADX", "OrionTrend",
-                    "ExpMove", "Cost", "SafetyFactor", "Score"
+                    "ExpMove", "Cost", "SafetyFactor", "Score",
+                    "ShiftScore", "ModeSuggest", "AtrRatio", "VolRatio", "AdxFlip", "FakeBreak",
+                    "ModeFinal", "Policy",
+                    "GateMinAdxPass", "GateMaxExpPass", "GateVolTrapPass", "BlockedReason",
+                    "ConfigMinAdx", "ConfigMaxExp", "ConfigVolTrap", "ConfigFee", "ConfigSlip", "ConfigSpread"
                 ])
                 
             votes_str = "|".join([f"{v.module}:{v.direction}({v.confidence:.2f})" for v in verdict.votes])
@@ -61,6 +67,35 @@ class Reporter:
             score = f"{meta.get('score', 0.0):.2f}"
             safety = f"{meta.get('safety_factor', 0.0):.2f}"
             
+            # MRIE Metadata
+            start_mrie = meta.get("mrie", {})
+            shift_score = f"{start_mrie.get('shift_score', 0.0):.2f}"
+            mode_suggest = start_mrie.get("mode_suggest", "N/A")
+            atr_ratio = f"{start_mrie.get('atr_ratio', 0.0):.1f}"
+            vol_ratio = f"{start_mrie.get('vol_ratio', 0.0):.1f}"
+            adx_flip = start_mrie.get("adx_flip", 0)
+            fake_break = start_mrie.get("fake_break", 0)
+            
+            # Router Metadata
+            router_meta = meta.get("router", {})
+            mode_final = router_meta.get("mode_final", mode_suggest) # Fallback to suggest
+            policy = router_meta.get("policy", "N/A")
+            
+            # Gate Metadata
+            gate_meta = meta.get("gates", {})
+            g_min_adx = gate_meta.get("min_adx_pass", 1)
+            g_max_exp = gate_meta.get("max_exp_pass", 1)
+            g_vol_trap = gate_meta.get("vol_trap_pass", 1)
+            blocked_reason = gate_meta.get("blocked_reason", "N/A")
+            
+            # Config Columns (Persistent)
+            c_min_adx = self.config.get("min_adx", 45.0)
+            c_max_exp = self.config.get("max_exp_move_bps", 0.0)
+            c_vol_trap = self.config.get("vol_trap_mult", 0.0)
+            c_fee = self.config.get("fee_bps", 4.0)
+            c_slip = self.config.get("slippage_bps", 2.0)
+            c_spread = self.config.get("spread_bps", 1.0)
+            
             writer.writerow([
                 verdict.timestamp,
                 verdict.regime,
@@ -82,11 +117,30 @@ class Reporter:
                 exp_move,
                 cost,
                 safety,
-                score
+                score,
+                shift_score,
+                mode_suggest,
+                atr_ratio,
+                vol_ratio,
+                adx_flip,
+                fake_break,
+                mode_final,
+                policy,
+                g_min_adx,
+                g_max_exp,
+                g_vol_trap,
+                blocked_reason,
+                c_min_adx,
+                c_max_exp,
+                c_vol_trap,
+                c_fee,
+                c_slip,
+                c_spread
             ])
 
     def log_reject(self, timestamp: float, symbol: str, reason: str, details: str, meta: str = ""):
         path = os.path.join(self.run_dir, "rejects.csv")
+        os.makedirs(self.run_dir, exist_ok=True)
         exists = os.path.exists(path)
         with open(path, 'a', newline='') as f:
             writer = csv.writer(f)
